reflexao.md

1. No código a parte que mais repetiu foi o style, porque foi colocado muita coisa direto no `App.jsx`.

2. Se caso a lanchonete tivesse 30 itens, o projeto ainda funcionaria porque o `map()` mostra todos os itens sozinho. Mas a tela ficaria maior e o código ia ficar mais bagunçado. Se fosse um projeto maior, seria melhor separar mais as coisas para ficar mais organizado.

-----------------------------------------------------------------------------------------------------------

1. o código ficou mais organizado
2. a cor iria mudar 1 lugar só
3. os dados chegaram por props, enviados do App.jsx para o ItemCardapio

----------------------------------------------------------------------------------------------------------

1. Uma variável comum não atualiza a tela porque o React não reconhece essa mudança.

2. O total ficou no `App.jsx` porque ele é o componente principal e controla todos os itens.

3. O botão avisa o `App` usando uma função passada por props.
